<script>
  $('#star-avg-<%= product.id %>').raty({
    size: 36,
    starOff:  '<%= asset_path('star-off.png') %>',
    starOn : '<%= asset_path('star-on.png') %>',
    starHalf: '<%= asset_path('star-half.png') %>',
    readOnly: true,
    score: <%= product.reviews.average(:score).to_f.round(1) %> ,
  });
</script>
